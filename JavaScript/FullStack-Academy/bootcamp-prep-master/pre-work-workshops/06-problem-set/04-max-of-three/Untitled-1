const myMnemonic = (...words) => {

    let mnemonic = '';

    for (word of words) {
        const currentWord = word;
        const currentChar = currentWord[0]
        mnemonic += currentChar;
    }

    return mnemonic;
}
function test() {
    
}