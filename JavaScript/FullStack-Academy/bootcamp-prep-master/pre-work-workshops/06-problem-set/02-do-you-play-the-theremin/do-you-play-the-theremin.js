// YOUR CODE BELOW
const doYouPlayTheTheremin = (text) => {
    if (text[0] === 's' || text[0] === 'S') {
        return true;
    } else {
        return false
    }
}

// const doYouPlayTheTheremin = word => (word[0] === 's' || word[0] === 'S') ? true : false;